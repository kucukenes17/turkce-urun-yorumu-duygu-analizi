# Hafta 1: veri yükleme + keşif (EDA) + baseline için bağımlılıklar.
# Transformer (torch/transformers) paketleri Hafta 2'de eklenecek.
datasets>=2.19
huggingface-hub>=0.23
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
matplotlib>=3.8
seaborn>=0.13
tabulate>=0.9
scikit-learn>=1.4
joblib>=1.3
gradio>=4.0
# Demo (app.py) fine-tune edilmiş BERTurk modelini kullanır:
transformers>=4.40
torch>=2.2
